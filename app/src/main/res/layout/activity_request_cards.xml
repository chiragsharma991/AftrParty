<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/llayout_requestant_head">

        <android.support.v7.widget.Toolbar
            android:id="@+id/drawerSettingsToolbar"
            android:background="@color/colorPrimaryDark"
            android:layout_alignParentTop="true"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content">

            <RelativeLayout
                android:layout_width="fill_parent"
                android:layout_height="44dp"
                android:id="@+id/toolbar_content"   >


                <TextView
                    android:textSize="@dimen/text_size"
                    android:layout_centerInParent="true"
                    android:text="Requestants"
                    android:id="@+id/activity_title"
                    android:textColor="#ffffff"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"   />
            </RelativeLayout>

        </android.support.v7.widget.Toolbar>
    </LinearLayout>

    <com.andtinder.view.CardContainer
        android:layout_below="@+id/llayout_requestant_head"
        android:id="@+id/layoutview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" />

    <ProgressBar
        android:layout_height="30dp"
        android:layout_width="30dp"
        android:id="@+id/pd"
        android:visibility="gone"      />

    <TextView
        android:layout_height="30dp"
        android:layout_width="wrap_content"
        android:id="@+id/txt_no_requests"
        android:textSize="20dp"
        android:textStyle="bold"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:visibility="gone"
        android:textColor="@android:color/black"
        android:text="There are no requests yet"        />
</RelativeLayout>
